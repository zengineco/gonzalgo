name = "gonzalgo"
version = "0.5.2"
# Reservoir renders this field as the package's description, so it has to say
# what the tool is for rather than how it works. It previously described the
# extractor, which made the listing read as a graph dumper.
description = "Axiom provenance for Lean 4: which step introduced an axiom, and whether the theorem's own statement required it. Finds theorems resting on an inherited sorry or on native_decide, and keeps statement dependencies apart from proof dependencies."
keywords = ["axioms", "provenance", "sorry", "native_decide", "dependency-analysis"]
homepage = "https://f-keys.com/gonzalgo/"
license = "Apache-2.0"
licenseFiles = ["LICENSE", "NOTICE"]
readmeFile = "README.md"
defaultTargets = ["Gonzalgo"]

# No dependency on Mathlib on purpose. This library is imported *into* whatever
# project is being measured, so pinning a Mathlib revision here would force a
# version on every user and make the package unusable for anyone on a different
# one. It needs only Lean itself.

[[lean_lib]]
name = "Gonzalgo"

[[lean_exe]]
name = "gonzalgo"
root = "Main"
# Imports the target module at runtime, so one binary works against any project
# Lake can build rather than needing a scratch file per project.
supportInterpreter = true
